<% if @post.persisted? %>
  <%= turbo_stream.prepend "posts-container", partial: 'post', locals: { post: @post, user: @user } %>
  <%= turbo_stream.remove "no-posts" %>
<% end %>
<%= turbo_stream.replace "flash" do %>
  <%= render 'shared/flash'%>
<% end %>
